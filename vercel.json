{
  "builds": [
    {
      "src": "src/*",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/bazar-front"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
